#{
#define CTIME_IMPLEMENTATION
#include "ctime.h"
char *comptime_sum(int a, int b) {
	int result = a + b;
	char *s = ct_format("%d", result);
	return s;
}
}#
#include <stdio.h>

int main() {
	printf("%d\n", $( comptime_sum(6, 7) )$);
	return 0;
}
